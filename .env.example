# OpenAI Provider 仅在 llm.enabled=true 且 provider=openai 时读取此环境变量。
# 请在本地 .env、进程环境或密钥管理系统中填写，禁止把真实值提交到 Git。
# 第三方 OpenAI 兼容中转站专用；使用官方 API 时保持下面两行注释状态。
# OPENAI_BASE_URL=https://relay.example.com/v1
# OPENAI_API_MODE=chat_completions
OPENAI_API_KEY=
